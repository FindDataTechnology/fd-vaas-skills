# unify-publish-runtime

统一发布运行时到 patchright: macOS 弃用 ego-browser egoScript, 全平台走 .py(patchright=Playwright); 删 6 个 .mjs + 死 lib/browser-utils.mjs; 修描述填充(execCommand->keyboard.type) + publish.mjs 遗留 bug
