"""Namespace tool modules for the unified VAAS MCP server.

Namespaces (prefixes): generate_ / orchestrate_ / publish_ / assets_ / registry_.
Tool names use underscores (MCP spec forbids dots); the prefix is the namespace.
"""
